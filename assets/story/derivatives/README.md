# Story derivatives (generated)

Generated by `npm run images` from the originals in `../originals/`. `manifest.json` records each image's natural size and the derivative widths. The build copies these files to `/img/story/` only when `story` is enabled, approved and public in `content/site.config.json`; otherwise nothing from this folder reaches the deployed site.
